/** @type {import('next').NextConfig} */
const nextConfig = {
  // Convex handles its own bundling
  experimental: {
    // Enables dynamic params for server components
  },
};

module.exports = nextConfig;
